# Node.js (only present when Level 2 build deps are active)
node_modules/
package-lock.json

# Vendor CSS/JS output is NOT ignored — committed for static deploys
# assets/css/vendor/
# assets/js/vendor/

